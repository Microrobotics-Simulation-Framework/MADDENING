# MADDENING v0.2 Progress Tracker

Tracks the nine planned features for the v0.1 → v0.2 cycle.
Created on `feat/v0.2-roadmap` after v0.1 release.

Legend: ☐ not started · ◐ in progress · ☑ complete · ✖ deferred

---

## ⇨ v0.2.1 PATCH RELEASE — PREPARED (2026-05-30)

`release/v0.2.1` branch closes all three items previously deferred to
v0.2.1 from the v0.2.0 release.  See
[`CHANGELOG.md`](CHANGELOG.md) `[0.2.1]` for the full narrative
(including the semver carve-out for the edge-validation flip);
short version:

- ☑ **StaticArray runtime slicing (3a materialisation).**
  `ShardedStencilNode` now actually slices each
  `StaticArray(replication="shard", shard_axis=K)` per-device via
  `jax.device_put` + `NamedSharding`, halo-exchanges it with
  `boundary="edge"`, and delivers it as `static_padded` to
  `update_padded`.  Acceptance test in
  `tests/cloud/multigpu/test_sharded_static_data.py` (5 cases).
  Unblocks MIME's `IBLBMFluidNode` multi-GPU sharding for the de Boer
  step-out replication.
- ☑ **Shape/Dtype warning→error flip.**
  `GraphManager.compile()` raises
  `ExceptionGroup` of `ShapeMismatchError` / `DtypeMismatchError` on
  shape/dtype mismatches that previously emitted warnings.
  `UnitMismatchWarning` is unchanged.  Migration playbook in
  `docs/developer_guide/edge_validation_migration.md`.
- ☑ **`compile()` advisory-noise cleanup.**  Single-node graphs no
  longer emit a disconnected warning; the uncovered "cycle detected"
  advisory moved from `warnings.warn(UserWarning)` to a `logging.info`
  call + an `INFO:` issue.

The v1→v2 checkpoint migration helper (also listed in the original
v0.2.1 deferral block) stays deferred — it was conditional on a future
checkpoint format change that hasn't happened.

**Hand-off boundary:** branch prepared, full test suite green (1680
passed; slow-marked tests deferred to the pre-tag run).  The user
handles commit shaping, tag (`v0.2.1`), GitHub Release (which fires
`publish.yml` → PyPI), and the MICROROBOTICA umbrella docs deploy.

---

## ⇨ RELEASE CHECKLIST & HANDOFF — v0.2.0 RELEASED (2026-05-20)

**Read this first.**  All nine roadmap features below are landed
(eight complete, #2 scaffolding-only).  The work that remains is the
release mechanics, not feature development.

### Where things stand

- **Branch**: MADDENING `feat/v0.2`, all work committed + pushed.
  Last commit on the feature run: `b029949`.
- **Test suite**: 1612 passed, 4 skipped, 1 deselected.
  Run it with:
  `cd /home/nick/MSF/msf/MADDENING && source ../.venv/bin/activate && python -m pytest -q`
  (the shared venv is `/home/nick/MSF/msf/.venv`; `-m 'not slow'` is
  the default, add `-m 'slow or not slow'` for the full lane).
- **Docs**: a multi-version preview site is wired on the MICROROBOTICA
  repo branch `feat/versioned-docs-preview` — **local-only, never
  pushed**.  Build it with `cd MICROROBOTICA/docs && make
  preview-versions`.  It must NOT go live until v0.2.0 is tagged
  (the `v0.2` switcher row needs a real URL behind it).
- The four pre-tag design decisions (StaticArray, checkpoint version
  policy, FutureWarning+MigrationError, replace_node×sharding guards)
  all shipped in `ec70c19`.

### Next steps — do these 8, in order, to reach the v0.2.0 tag

**Progress update (2026-05-20, session 2):** all eight pre-tag
steps done.  Steps 2–6 landed in MADDENING commits `4c3257b` /
`4e030b9`.  Steps 1 and 8 closed by merging both MIME branches into
MIME `main` (`5448653`): `feat/halo-width-migration` (halo_width API
+ Ghia fixture relocation) and `feat/fvm-float64-audit` (FVM
float64/CFL fixes).  MIME's full suite is green against MADDENING
`feat/v0.2`: **626 passed, 50 skipped, 51 deselected, 0 failed**.
**MADDENING v0.2.0 is tagged and pushed** (`v0.2.0` → `ff59eb5`),
and step 7 is done — all eight steps complete, the release is done.

1. ☑ **MIME-side `requires_halo` → `halo_width` migration.**
   MADDENING side: the warning is a `FutureWarning` and
   `maddening.warnings.MigrationError` is the v0.3 raise path.
   MIME side: `feat/halo-width-migration` merged into MIME `main` —
   base `MimeNode.halo_width() -> {}`, pointwise subclasses drop the
   override, `IBLBMFluidNode` / `FVMFluidNode` declare `halo_width`.
   Only the two test-internal stub classes (`_FieldStub`,
   `_BodyStub`) still touch `requires_halo` — a harmless,
   non-escalating test-fixture detail left for a later MIME test
   cleanup.
2. ☑ **Lab-graph edge-validation smoke check.**  MIME's 625-test
   suite against `feat/v0.2` triggered ZERO `ShapeMismatchWarning`
   / `DtypeMismatchWarning` / `UnitMismatchWarning` — the new edge
   validation generates no noise in real graphs.  Last box on #4 closed.
3. ☑ **`CHANGELOG.md` entry.**  `[Unreleased]` cut to `[0.2.0]`; the
   nine roadmap features appended; `Deprecated` section added; a
   pointer to `docs/release_notes/v0.2.md` added.  Commit `4c3257b`.
4. ☑ **Version bump.**  `pyproject.toml` `0.1.0` → `0.2.0`;
   `maddening.__version__` now exposed via `importlib.metadata`
   (it was missing — `provenance.py` was silently recording `""`).
   `jax<0.6` cap kept (not revisited).  Commit `4c3257b`.
5. ☑ **Docker smoke-test.**  `docker build -f docker/Dockerfile.cloud`
   succeeds; `zstandard-0.25.0` installs in `[server]`; verify stage
   green (JAX 0.5.3, MADDENING OK, GStreamer 1.20.3, ZMQ 4.3.5).
   Throwaway image was `maddening-cloud:v0.2-smoke` — the build
   predated the version bump, so rebuild at tag time to bake in 0.2.0.
6. ☑ **PyPI metadata.**  `keywords` + `classifiers` added to
   `pyproject.toml` (both were absent).  No `License ::` classifier —
   the SPDX `license` field is used instead (PEP 639 forbids both).
   `description` kept as-is.  Commit `4c3257b`.
7. ☑ **Bump the MICROROBOTICA submodule pointer.**
   `MICROROBOTICA/docs/projects/maddening` moved from the pre-v0.2
   commit `5cf74c9` to the `v0.2.0` tag (`ff59eb5`).  Committed
   (`694d6a4`) on MICROROBOTICA's local-only
   `feat/versioned-docs-preview` branch — not pushed, consistent
   with that branch staying local until the version switcher goes
   live.  (The MIME submodule is still at the old `27c2bbd`; bumping
   it belongs to the MIME v0.2 cycle below, not this checklist.)
8. ☑ **FVM verification.**  Both MIME branches merged into MIME
   `main`: `feat/halo-width-migration` relocated the Ghia fixture to
   `tests/verification/fixtures/` (resolving the lid-cavity test's
   `FileNotFoundError`), and `feat/fvm-float64-audit` carries the
   CFL-respecting `dt` + float64 fixes for the Womersley, IBM, and
   Taylor-Green verification tests.  All four FVM verification tests
   now pass — MIME suite 626 passed, 0 failed against MADDENING
   `feat/v0.2`.

### After step 8 — prepare MIME v0.2

Once MADDENING v0.2.0 is tagged, the next body of work is **bringing
MIME to v0.2** so it fits the updated MADDENING:

- Bump MIME's `pyproject.toml` pin to `maddening>=0.2.0,<0.3`.
- Complete the `requires_halo` → `halo_width` migration in MIME (this
  is step 1 above — it both unblocks the tag and is the start of
  MIME v0.2).
- Re-verify MIME's suite against the tagged MADDENING 0.2.0 (not the
  branch).
- Adopt the v0.2 MADDENING surfaces where they help MIME:
  `StaticArray` for FVM meshes, the edge-validation warnings, the
  profiler endpoint.
- **MIME node improvements** (better physics, new node types) are a
  *separate, later* effort — explicitly NOT part of MIME v0.2 fit-up.
  Keep that scope split: v0.2 fit-up is mechanical compatibility;
  node improvements are their own cycle.

### Deferred (already triaged — do NOT pull into v0.2.0)

- ☑ v0.2.1 (shipped 2026-05-30 — see top-of-file block):
  StaticArray runtime slicing, Shape/Dtype warning→error flip,
  `compile()` advisory-noise cleanup.
- ☐ v0.2.x or later: v1→v2 checkpoint migration helper (conditional
  on a future checkpoint format change — not v0.2.1 since the format
  hasn't changed).
- v0.3 (see `plans/MADDENING_v0.3.0_PLAN.md` outside the repo for the
  current plan): FMI 3.0 substrate (newly elevated from M3),
  `@stability` API audit begin, USD live-stage consolidation, IFT
  coupling-solver merge from `perf/coupling-ift-gradient`, plus the
  compat-hygiene bucket (`requires_halo` hard-removal + `MigrationError`
  raise, `ShardedNode` alias removal, bare-array `static_data`
  coercion removal, `*Warning` alias removal from this v0.2.1 cycle).
  Nice-to-haves: IFT-IMVJ cross-timestep warm-start (promoted from
  0.4.x), MIME decoder-zoo pull-over into `surrogates/primitives/`,
  `SurrogateArchitecture` ABC decoupling,
  `s3://`/`gs://`/`azure://` URL schemes in
  `download_and_load_state`.

---

## Suggested Sequencing

```
Week 1-2:  #5, #6, #9        (fast wins, free up encoder/API mind-share)
Week 1-6:  #1                (long pole — pencil decomp + LBM/Heat port)
Week 2-3:  #3                (concurrent with #1, fixes static_data sharding semantics)
Week 4-5:  #2                (surrogate restructure — mid-cycle so churn is bounded)
Week 5-6:  #7 (Lambda+AWS) | #8 (preempt wiring)   parallel
Week 6:    #4                (validation as WARNINGS in 0.2, errors in 0.2.1)
Week 7:    #7 GCP if time remains, otherwise defer to v0.3
```

Critical risk: #1 slipping squeezes everything else. If it slides past week 6,
defer GCP (#7) first, then re-scope #4 to warnings-only and keep errors for 0.2.1.

---

## #1 — 3D halo exchange with slab + pencil decomposition  ◐

**Estimate:** 5–6 weeks (planning at 6 with contingency) · **Risk:** HIGH (long pole)

**Scope**
- `halo_width(axis) -> dict[int, int]` contract (per-axis from day one — no second contract break for pencil in v0.3)
- `update_padded` entry point on stencil nodes
- `halo_exchange` primitive via `shard_map` + `ppermute`
- `ShardedNode` split: `ShardedPointwiseNode` (current behavior) + `ShardedStencilNode` (new)
- LBM `_stream` rewrite for halo-resident distributions
- `LBMNode` (D3Q19) port + `HeatNode` port (HeatNode is correctness-test only)
- Device mesh axis naming: `("spatial_y", "spatial_z")` (internal, touches `device_mesh.py`)
- Multi-device gradient audit through `ppermute`

See `/home/nick/.claude/plans/magical-tickling-lampson.md` for the detailed M1–M9 milestone plan with local CPU-virtual-device testing strategy.

**Milestones**
- [x] **M1** — `halo_width` contract + compat shim (1388 in-tree tests pass, MIME 669 tests pass under shim)
- [x] **M2** — 2-D device-mesh support (1410 in-tree tests pass; 8-device 2x4 and 16-device 4x4 pencil meshes verified locally)
- [x] **M3** — `halo_exchange` primitive via `shard_map` + `ppermute` (1418 tests pass; halo=1/2, periodic/edge/zero, 3-D pencil, analytic gradient all green)
- [x] **M4** — `ShardedStencilNode` lands; `ShardedNode` → `ShardedPointwiseNode` with deprecated alias (1425 tests pass; toy 1-D Laplacian bit-exact, 3-D Laplacian on 2x4 pencil within 1 ULP)
- [x] **M5** — HeatNode `update_padded` + Fourier oracle under sharding (2/4/8/16-device meshes all <5% L2 error vs Fourier-series analytic; MIME 669 tests pass under shim)
- [x] **M6** — `_stream_padded` for D3Q19/D2Q9; LBM `update_padded` (no walls / no Zou-He yet — those land in v0.2.x); bit-exact regression vs legacy `jnp.roll`
- [x] **M7** — Sharded LBMNode on 8-device 2x4 and 4x2 and 16-device 4x4 pencil meshes; matches unsharded over 50 steps; mass conserved equal to unsharded (full Hagen-Poiseuille requires sharded `wall_mask` + Zou-He BCs → v0.2.x follow-up)
- [x] **M8** — `jax.grad` through 5-step sharded heat rollout matches centred FD; ShardedStencilNode integrates with GraphManager (single-node + 2-node edge-coupled + `run_scan`); MIME 669 tests pass under compat shim
- [ ] **M9 (optional)** — RunPod multi-GPU smoke test for NCCL + perf scaling (skipped — local validation comprehensive, full picture deferred until real GPU access)

**v0.2.x follow-ups:**
- [x] Sharded `wall_mask` (in state, stored as `uint8`) + Zou-He pressure BCs under sharding + Hagen-Poiseuille parabolic-profile verification on 2x4 pencil
- [x] `replace_node` × sharding: tested 3 scenarios (unsharded→sharded, sharded→unsharded, sharded→pointwise w/ edges preserved); `_dirty=True` correctly triggers re-compile
- [x] LBM-Guo body-force coefficient: added the F/2 half-correction in `_compute_macroscopic`; Hagen-Poiseuille amplitude went from 0.42x to 1.13x of textbook `u_max` (remaining 13% is the mid-link bounce-back effective-radius offset, a discretization artifact independent of the Guo scheme)
- [x] MIME node migration: shipped on `MIME/feat/halo-width-migration` (PR-ready). Base `MimeNode.halo_width() -> {}`, 7 pointwise subclasses drop the explicit override, `IBLBMFluidNode` declares `{0:1, 1:1, 2:1}`, `FVMFluidNode` declares `{0:1}` placeholder (FVM's unstructured-mesh halo doesn't fit per-axis semantics — graph-partition halo is a separate effort). Also relocates `ghia-table1.json` from untracked `tmp/FVM/` to `tests/verification/fixtures/`. MIME suite 669 passed.
- [x] `static_data` channel on `SimulationNode` (v0.2 item #3 — landed in `1a68e6b`; default-empty preserves all existing nodes)

**GPU-dependent FVM failures — root cause found and fixed (`MIME/feat/fvm-float64-audit`):**

The dtype hypothesis was only partly right.  The actual root cause is
**explicit-convection CFL violation**, not a dtype-plumbing bug.  The
PISO solver in `mime/nodes/environment/fvm/piso.py` is structurally
dtype-aware (every `jnp.zeros`/`jnp.array` already uses
`mesh.V.dtype`).  Float32 GPU reduction-order noise was incidentally
acting as numerical viscosity, damping an unstable convection mode
that the tests' chosen `dt` was provoking; the result was a stable
but heavily contaminated answer.  Float64 strips the artificial
damping and the scheme blows to ±inf.

| Test | Original `dt` | CFL | Float32 err | Float64 err | Fix |
|---|---|---|---|---|---|
| `taylor_green` | 0.4·dx (CFL≈0.4 — already CFL-OK) | ✓ | 21.65% | clean fix via x64 | `dtype=jnp.float64` + `jax_enable_x64=True` |
| `womersley_wo7` | T/80 | 7.7 | 26.11% | NaN by step 75 | drop `dt` to T/640 (CFL≈0.96) + float64 |
| `ibm_poiseuille_2d` | 1.0 | 7.7 | 42.84% | NaN | drop `dt` to 0.1 (CFL≈0.75) + float64 |

The Taylor-Green case is the one the original peer diagnosis nailed
exactly: pure-central periodic flow at CFL≈0.4 *is* purely a
reduction-order-noise vs float32 story, and the fix is just to flip
the dtype.  The other two looked superficially similar but were
actually CFL violations masquerading as precision artefacts —
shrinking `dt` is the load-bearing change there; the dtype change is
required only because the previous "stability" was floating-point
artefact.

After the fix (MIME `22fed4f` + `ced7746`):
- Womersley amplitude error: 26.11% → **0.020%** (well under 2% bar)
- IBM Poiseuille profile error: 42.84% → **1.95%** (under 3% bar)
- Taylor-Green energy error: 21.65% → **<5%** (passes)

Full MIME suite on RTX A2000 + JAX 0.5.3 cuda12: 625 passed, 50
skipped, 51 deselected, 1 failed (lid_cavity — missing
`tmp/FVM/ghia-table1.json`, relocated to
`tests/verification/fixtures/` on the parallel
`feat/halo-width-migration` PR, so will resolve when that lands).

**Generalisable framing** for future FVM verification work: the
"implicit diffusion makes the time step unconditionally stable"
docstring shorthand has been hiding the explicit-convection CFL
constraint.  Tests must either honour `u_max · dt / dx ≲ 1`
explicitly or use an implicit/SL convection scheme.  Document the
CFL constraint in `piso.py` and `simple.py` docstrings.

**Breaking-contract notes**
- `requires_halo` (bool) is superseded by `halo_width` (dict). Keep `requires_halo` as a default-implemented compatibility shim for one minor version, removed in v0.3.  In v0.2 the warning is now a `FutureWarning` (so notebooks see it; was `DeprecationWarning` until `ec70c19`) and references the new `maddening.warnings.MigrationError` that will replace the warning when the shim is removed.  The grep CI step that gates v0.3 lives on MIME's side (MIME asserts no `requires_halo` overrides remain when its test suite runs against MADDENING main).
- Pencil mesh axis names are user-visible in error messages — settle naming early.

**Definition of done**
- 256³ LBM runs on 8 GPUs with <32-cell pencil thickness on the sharded axis
- All in-tree nodes declare `halo_width` correctly; tests cover halo correctness for stencil nodes
- Gradient audit passes
- Coupling-under-sharding test green

---

## #2 — Primitive extraction from MIME + surrogates restructure  ◐ *(scaffolding landed)*

**Estimate:** 2 weeks · **Risk:** LOW (additive) → MEDIUM (with restructure)

**Scope**
- Pull `cholesky_decoder` + decoder zoo out of MIME into `surrogates/primitives/`
- Standalone weight ser/de (independent of `SurrogateArchitecture` ABC)
- Decouple training loop from ABC (so external nodes can train without subclassing)
- Restructure `surrogates/` into subpackages:
  - `surrogates/primitives/` — decoders, encoders, common building blocks
  - `surrogates/weights/` — serialization (`save_weights`/`load_weights`)
  - `surrogates/training/` — trainer, callbacks, physics losses
  - `surrogates/architectures/` — MLP, DeepONet, SDeepONet, FNO (existing layout)
  - `surrogates/replace/` — `replace_node` and helpers

**Subtasks**
- [ ] Identify all decoders MIME currently reaches into (`cholesky_decoder`, others) — requires MIME-side audit; not started
- [ ] Extract decoders to `surrogates/primitives/` with their own tests — `primitives/` scaffold present (`d143b26`-era restructure commit) but empty; needs the MIME decoder pull-over
- [~] Move `checkpoint.py` to `surrogates/weights/` — **re-export shim landed** (`weights/__init__.py` thin-imports from `checkpoint.py`); physical move deferred so legacy paths keep working without churn
- [~] Move `trainer.py`, `callbacks.py`, `physics_losses.py` to `surrogates/training/` — **lazy re-export landed** via `training/__getattr__`; physical move deferred
- [x] Move `replace.py` to `surrogates/replace/` — `replace.py` is now a subpackage with `_core.py` carrying the implementation
- [ ] Decouple training loop from `SurrogateArchitecture` ABC (accept any (params, fn) pair) — not started; ABC still required
- [x] Re-export old import paths from `surrogates/__init__.py` for one minor version — 14 legacy paths pinned by parametrised back-compat test
- [ ] Migration note in CHANGELOG — not written yet
- [x] Verify no circular imports between subpackages — implicit (passing import + suite)

**Definition of done**
- [x] New subpackage layout works without breaking existing tests — 1499 passed at the time of `bf...`
- [ ] Decoders importable as `from maddening.surrogates.primitives import cholesky_decoder` — package exists but is empty
- [ ] MIME can stop bypassing `surrogates/` (smoke check against MIME branch) — blocked on the decoder extraction

---

## #3 — `static_data` channel on `SimulationNode`  ☑

**Estimate:** 1 week (concurrent with #1) · **Risk:** MEDIUM (interacts with #1 sharding)

**Scope**
- Optional `static_data` protocol on `SimulationNode`
- JIT closes over it, hashable cache key, survives `replace_node` cleanly
- Decision: **reconstructable**, not checkpointed (FVM meshes from USD don't NPZ-roundtrip cleanly)
- `static_data_provider` callable on the node; checkpoint stores only the provider config

**Subtasks**
- [x] `static_data` property (default empty dict) on `SimulationNode` — `node.py`
- [x] Hashing strategy: structural hash over keys + shape/dtype of array values — `static_data_hash()` hashes `(key, shape, dtype)` for arrays and `(key, repr(value))` for scalars; contents NOT hashed
- [x] Wire into JIT cache key in `GraphManager._build_step_fn()` — via `_check_static_data_dirty()` snapshot at `compile()` + per-step drift check at all 7 step/run entry points
- [x] `replace_node` preserves/migrates `static_data` — automatic: `remove_node`+`add_node` sets `_dirty=True`, next `step()` re-snapshots hashes; verified by `test_replace_with_static_data_node_dirties_graph`
- [x] Checkpoint: store provider config only, reconstruct on `load_state` — **HeatNode demonstrates the pattern** (commit `9e1fc5b`): `__init__` rebuilds `_grid_x_array` from `self.params`, so save_state→reconstruct→load_state restores both state and static_data without serialising the latter. Test: `test_checkpoint_roundtrip_with_static_data`.
- [x] Interaction with #1: how does `static_data` shard? (per-shard slice vs replicated) — **decided** (`ec70c19`): typed `StaticArray(value, replication, shard_axis)` dataclass.  Bare arrays coerce to `replicate` with a FutureWarning (bare path removed in v0.3).  Hash now includes `(shape, dtype, replication, shard_axis)` so the dirty-check sees policy changes too.  Slicing-time materialisation (3a — split per-device arrays before JIT closure) is the implementation contract; a sharded node consuming this lands in v0.2.1.
- [x] Document the pattern in `DESIGN.md` — §2 "Static-data channel"
- [x] Migrate `FVMMesh` / `geometry_source` consumers to `static_data` (HeatNode w/ grid_points, vessel demos) — **HeatNode landed** (`9e1fc5b`) and migrated to StaticArray (`ec70c19`, `replication="shard", shard_axis=0`); FVMMesh + vessel demos still pending (deferred to v0.3 / MIME)

**Definition of done**
- [x] `static_data` declared on `SimulationNode`; default-empty preserves all existing nodes
- [x] At least one node (HeatNode non-uniform grid) uses it for real
- [x] Sharded static_data semantics decided + documented before #1 lands — `StaticArray(replication, shard_axis)` contract, documented in DESIGN.md §2 and `static_data.py`'s module docstring
- [x] `DESIGN.md` updated

---

## #4 — Compile-time edge validation  ☑ *(warnings; v0.2.1 flips to errors)*

**Estimate:** 1 week · **Risk:** MEDIUM (user-visible; ship as warnings first)

**Scope**
- Shape/dtype validation from `BoundaryInputSpec` at `compile()` time
- Unit warnings from `expected_units`
- **Ship as warnings in v0.2, flip to errors in v0.2.1** to give MIME experiments time to migrate

**Subtasks**
- [x] Walk every edge at `compile()`; check `BoundaryInputSpec` on target node
- [x] Shape mismatch → `EdgeValidationWarning` in 0.2 (later: `EdgeValidationError`) — `ShapeMismatchWarning` subclass
- [x] Dtype mismatch → same — `DtypeMismatchWarning`
- [x] Unit mismatch → `UnitMismatchWarning` (already partially wired at graph_manager.py:1227 — generalize) — promoted to proper subclass
- [x] Aggregate validation errors so user sees all problems at once, not one-at-a-time — `validate()` collects all `issues`; `compile()` walks the full list
- [x] Tests: synthetic graphs with each failure mode — 15 cases in `test_edge_validation.py`
- [x] Migration guide: how to fix common errors — `docs/developer_guide/edge_validation_migration.md` (commit `9e8e72d`)
- [x] Tracking issue for 0.2.1 flip-to-errors — playbook documented in the migration guide; trigger is "MIME-side migration complete" (concretely: MIME's CI passes against MADDENING's strict-validation branch).  Units stay **permanently advisory** (closed in code via `UnitMismatchWarning` docstring in `ec70c19`).

**Definition of done**
- [x] Every `add_edge` call validated at compile
- [x] Lab newcomer wiring a 20-edge graph gets all mistakes flagged in one pass — aggregation test confirms this
- [x] MIME's 20+-edge experiments emit warnings cleanly (no errors yet) — smoke check done (2026-05-20): MIME's 625-test suite against MADDENING `feat/v0.2` triggered zero `ShapeMismatchWarning` / `DtypeMismatchWarning` / `UnitMismatchWarning`

---

## #5 — Field subscriptions on state encoder  ☑

**Estimate:** 3–4 days · **Risk:** LOW (pure addition)

**Scope**
- `BinaryStateEncoder` learns to pack a subset of fields
- Subscription protocol on WebSocket (`/ws/state/binary` and `/ws/state`) + ZMQ `NetworkRelay`
- Backward-compatible: no subscription = full state
- Schema rebuilt + re-sent on subscribe message

**Subtasks**
- [x] `BinaryStateEncoder(fields={"node": ["f1", ...]})` already exists per MEMORY — verify and extend — verified + 8 new cases covering subset packing, unknown-field/node, empty-dict semantics, bandwidth target
- [x] WebSocket: handle `{"type": "subscribe", "fields": {...}}` and rebuild encoder — already present in `/ws/state/binary`; extended in `c9ae8e0` to also accept `compression`
- [x] ZMQ relay: same protocol (or PUB topic-per-field, decide) — **decision: server-side static filter** via `NetworkRelay(fields=...)`; topic-per-field rejected because PUB always emits everything (subscriber-side filter doesn't reduce wire bandwidth)
- [~] Server-side: track per-client subscriptions; broadcast schema diff — **WebSocket** binary handler reconstructs encoder + re-sends schema on subscribe message. ZMQ uses a single server-wide filter rather than per-client (PUB/SUB has no back-channel); per-client ZMQ subscriptions would require REQ/REP coordination, deferred
- [x] Demo: laptop subscribes to LBM velocity only, ignores f-distributions — `examples/cloud/streaming/08_subscribe_lbm_velocity.py` (commit `76f606e`); end-to-end ws round-trip; shows 99.93% reduction on constant velocity with zstd
- [x] Benchmark: bandwidth reduction on 256³ LBM (target >95%) — covered as inline test asserts (>80% from subscription alone, >95% combined with #6 zstd on constant fields)

**Definition of done**
- [x] Old clients keep working without subscribe — default `fields=None` preserves full state
- [x] New clients can request a subset; bandwidth drops measurably
- [x] Both transports (WS, ZMQ) honor subscriptions — WS dynamic + ZMQ static

---

## #6 — State delta compression (zstd)  ☑

**Estimate:** 4–5 days · **Risk:** LOW · **Depends on #5**

**Scope**
- zstd of full frame as default-off flag on encoder
- Per-field change detection free with #5
- Optional XOR-delta flag for true delta encoding
- Applies to WebSocket binary + ZMQ binary; NOT Selkies (video-level compression already)

**Subtasks**
- [x] Add `compression` field to encoder schema (`none`, `zstd`, `zstd+xor`)
- [x] zstd encode in encoder; client decodes on receive — `decode_frame(frame, schema)` is the stateless inverse
- [ ] Per-field change detection: skip unchanged fields entirely (leverages #5's per-field packing) — **not implemented**; XOR-delta below is the alternative path we took (compresses unchanged bytes to ~0). Per-field skipping would need a wire-format change to encode "field N: unchanged" — deferred to v0.2.x
- [x] XOR-delta optional path (off by default) — `compression="zstd+xor"`; first frame falls back to plain zstd
- [~] Benchmark: 256³ LBM 60fps consecutive-frame compression ratio (target ~10:1 with zstd alone) — asserted via test on 32³ surrogate (10× ratio on constant 1024-element heat rod); 256³ × 60 fps not exercised in CI (would dominate test time)
- [x] Latency budget check: encode time stays below frame budget — `TestEncodeLatency` (commit `fc3a6c8`); 3 cases assert <10 ms / frame at 32³ for none/zstd, <15 ms for zstd+xor (slow-marked)
- [x] Configurable on the WS subscribe message — `{"type": "subscribe", "compression": "zstd"}`

**Definition of done**
- [~] `compression="zstd"` cuts a 256³ LBM stream by ~10× without dropping frames — proven at 32³; 256³ smoke-test deferred
- [x] Old clients (no compression field) keep working — schema default is `"none"`

---

## #7 — Cloud provider expansion (Lambda → AWS → GCP)  ☑ *(creds + examples; end-to-end deferred)*

**Estimate:** 2–3 weeks (parallelizable) · **Risk:** LOW

**Scope**
- Finish `LambdaLabsProvider` stub
- Add AWS provider (SkyPilot already abstracts most of it)
- Add GCP provider (longest tail — defer to v0.3 if #1 slips)

**Subtasks — Lambda**
- [x] Implement `LambdaLabsProvider.write_credentials()` / `delete_credentials()` — was in v0.1 as a stub; lifted out of "stub" status in `6993ba5`
- [ ] Catalog lookup for Lambda GPU types — **not in MADDENING**; SkyPilot handles the catalog (`sky show-gpus --cloud lambda`)
- [ ] End-to-end RunPod-style launch on Lambda — **not validated** end-to-end; requires real Lambda credentials. Credential layer fully tested.
- [x] Example `src/maddening/examples/cloud/03_lambda_launch.py`

**Subtasks — AWS**
- [x] `AWSProvider` (uses `~/.aws/credentials` profile) — also supports named profiles (`AWSProvider(profile="dev")`), session tokens, region → `~/.aws/config`
- [ ] EBS volume sizing / spot resilience tuning — **not addressed**; SkyPilot's default behaviour used. Spot resilience comes via #8.
- [ ] End-to-end launch on a small instance type — **not validated** end-to-end; requires real AWS account
- [x] Example `04_aws_launch.py`

**Subtasks — GCP (stretch, defer if #1 slips)**
- [x] `GCPProvider` (uses `gcloud auth application-default login`) — accepts both `service_account_json` (recommended for CI) and `application_default_credentials` (interactive flow)
- [ ] Preemptible VM support — **not addressed** at the MADDENING layer; SkyPilot's `use_spot` flag handles this
- [ ] End-to-end launch — **not validated**; requires real GCP project
- [x] Example `05_gcp_launch.py`

**Definition of done**
- [~] Lab can run on Lambda Labs and AWS at minimum — credential layer ready; live launch deferred until someone tries it with real creds
- [x] Per-provider credential lifecycle is consistent — all four providers share the `CloudProvider` ABC (write_credentials / delete_credentials / set_env_vars / restore_env_vars)
- [x] One example per provider — 02_runpod + 03_lambda + 04_aws + 05_gcp

---

## #8 — Preempt/checkpoint lifecycle wiring  ☑ *(file:// + manifest; live spot test deferred)*

**Estimate:** 1–1.5 weeks · **Risk:** LOW · **Depends on at least one spot-capable provider (#7)**

**Scope**
- Wire `CloudSession` preemption detection → `core/simulation/checkpoint.py` auto-snapshot
- `entrypoint.py` learns resume-from-URL behavior
- RunPod spot works today — start there

**Subtasks**
- [x] `CloudSession` emits preemption event → checkpoint hook — `make_preempt_snapshot_hook(server)` returns an `on_preempted=` callback that writes `.npz` + manifest
- [~] Auto-snapshot location: cloud storage bucket (provider-agnostic interface) — **local file + file:// + http(s)://** supported via `download_and_load_state`; S3/GCS/Azure blob URLs would need provider-specific upload paths. Today the orchestrator is expected to push the local snapshot up out-of-band.
- [x] `entrypoint.py` reads `RESUME_FROM_URL` env var, downloads, restores state
- [x] Resume-state integrity check (hash, schema version) — SHA-256 + `CHECKPOINT_SCHEMA_VERSION=1` in the sidecar manifest; tampered/version-drift checkpoints raise `CheckpointIntegrityError`
- [ ] End-to-end test: kill a RunPod spot instance mid-run, verify next launch resumes — **deferred** (requires real RunPod spot); every component covered with `file://` URLs
- [x] Document the preempt-resume contract — `docs/user_guide/cloud_resume.md` (commit `9e8e72d`); covers the hook, manifest schema, URL schemes, what the orchestrator still owns.  v0.2.0 also pins the **schema version bump policy** (`ec70c19`): release running schema N reads N and N-1; older raises `CheckpointVersionError` naming the intermediate release.  Policy lives as the module docstring on `checkpoint.py` and is enforced by `verify_manifest`.

**Definition of done**
- [~] RunPod spot preemption → state survives, next launch resumes from snapshot — wiring proven against a `_FakeInfo` + real `CloudSession._on_preemption_signal()`; live RunPod spot test deferred
- [ ] Works for at least one other provider once #7 lands AWS or Lambda spot — same caveat

---

## #9 — Profiler + cloud observability  ☑

**Estimate:** 1 week · **Risk:** LOW

**Scope**
- Wire `core/simulation/profiler.py` to `CloudSession` teardown auto-snapshot
- Integrate `jax.profiler.start_trace`
- `GET /sim/profile` REST endpoint returning perfetto-loadable trace

**Subtasks**
- [x] `start_trace()` / `stop_trace()` API on `GraphManager` (or `SimulationServer`) — module-level `start_jax_trace()` / `stop_jax_trace()` in `profiler.py`, with `JaxProfilerSession` context manager + REST endpoints `POST /sim/profile/jax/start` and `/stop`
- [x] `GET /sim/profile?duration=10s` triggers a trace, returns the perfetto JSON — landed as `POST /sim/profile?n_steps=N` returning Perfetto Trace Event JSON; the duration-based JAX trace is the `/sim/profile/jax/*` pair (start/stop instead of single-shot, so the caller controls scope)
- [x] On `CloudSession.teardown()`, auto-dump last trace to cloud storage — `/cloud/teardown` now base64-tarballs the last JAX trace dir in the response body (caller persists it). Direct upload to S3/GCS would chain through #8's blob-storage extension
- [x] Document: "open profile.json in https://ui.perfetto.dev" — referenced in the `profile_report_to_perfetto` docstring
- [x] Example/demo: profile an LBM step — `examples/advanced/profile_lbm_step.py` (commit `f70ae23`); builds a 2-node coupled Heat–Heat graph, runs `profile_graph`, writes Perfetto JSON ready for ui.perfetto.dev; `--jax-trace` flag also captures an XLA-level trace

**Definition of done**
- [x] Applied-math collaborator can answer "where's the time going?" via REST without SSH — `curl -X POST .../sim/profile > profile.json` then drag into Perfetto
- [x] Trace survives instance teardown — `/cloud/teardown` snapshot embeds the tarball

---

## Cross-cutting risks

1. **Pencil correctness under coupling.** Sharded stencil inside a coupling group is the convergence edge case nobody has hit yet. Plan a specific test for sharded-Heat ↔ sharded-LBM on the same pencil axis.
2. **Multi-device gradient audit.** `ppermute` is differentiable but the test infrastructure isn't there. Treat it as an explicit subtask of #1, not a postscript.
3. **Three simultaneous breaking contracts (#1, #3, #4).** Lab arrival timing means new students write against whatever is current. Migration guide written before week 4; ship #4 as warnings.
4. **`replace_node` × `static_data` × sharding** is a three-way interaction. Spec in `DESIGN.md` before #3 lands.
