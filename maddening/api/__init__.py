"""
MADDENING HTTP/WebSocket API.

Exposes the ``GraphManager`` over FastAPI with REST endpoints for
graph construction, simulation control, and a WebSocket stream for
real-time state snapshots.

Requires ``fastapi`` and ``uvicorn`` (optional dependencies).
"""
