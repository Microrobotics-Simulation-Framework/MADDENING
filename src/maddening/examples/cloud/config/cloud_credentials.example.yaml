version: 1
# Copy to ~/.maddening/cloud_credentials.yaml and fill in your keys.
# NEVER commit the filled-in file.
#
#   mkdir -p ~/.maddening
#   cp src/maddening/examples/cloud/config/cloud_credentials.example.yaml ~/.maddening/cloud_credentials.yaml

runpod:
  api_key: "rp_xxxxxxxxxxxx"

lambda_labs:
  api_key: "llxxxxxxxxxxxxxxxxxx"
