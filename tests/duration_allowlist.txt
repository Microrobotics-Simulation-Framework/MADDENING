# Tests allowed to exceed the default lane's time budget.
#
# Read by scripts/report_test_durations.py in CI (.github/workflows/ci.yml).
# One pytest node id per line, then " # " and the reason. The budget, from
# docs/developer_guide/testing_standards.md:
#
#   up to 1 s   fine
#   1-5 s       watch list; optimise when you are in the file
#   over 5 s    mark @pytest.mark.slow (still runs 3x a week in
#               slow-tests.yml), make it much faster, or keep it here with
#               "kept: <why it must run on every push>"
#
# An unlisted test over 5 s gets a warning on the run; over 20 s it fails the
# job. The 4x gap is runner noise: the same test varies 1.7x between runs at
# the median, 3x at the 90th percentile.
#
# "pending triage" entries were already over 5 s in at least one of eight
# green lane-runs (4 runs x 2 JAX lanes, release/0.4.0, 2026-09-24) when the
# gate arrived. Each is to be marked slow, sped up, or kept with a reason.
# This list only shrinks.
#
# The entries at the end came from the gate's own first run (PR 132, merged
# 2026-09-24): tests added or slowed since the eight lane-runs above, over
# 5 s on at least one lane. The first of them ran within 0.2 s of the 20 s
# hard line, so they are listed rather than left to flake.

tests/core/test_coupling_diagnostics_leave_the_state_alone.py::test_diagnostics_leave_the_returned_state_bit_identical[gauss-seidel-interface-ift] # kept: diagnostics=True must leave the returned state bit-identical, a claim a compiler rewrite can break and only a bit-for-bit comparison on every jaxlib sees; each ift cell compiles the spectral and gradient-bound machinery once (~6 s cold, nearly all XLA compile)
tests/core/test_coupling_diagnostics_leave_the_state_alone.py::test_diagnostics_leave_the_returned_state_bit_identical[jacobi-interface-ift] # kept: diagnostics=True must leave the returned state bit-identical, a claim a compiler rewrite can break and only a bit-for-bit comparison on every jaxlib sees; each ift cell compiles the spectral and gradient-bound machinery once (~6 s cold, nearly all XLA compile)
tests/core/test_coupling_diagnostics_leave_the_state_alone.py::test_diagnostics_leave_the_returned_state_bit_identical[jacobi-l2-ift] # kept: diagnostics=True must leave the returned state bit-identical, a claim a compiler rewrite can break and only a bit-for-bit comparison on every jaxlib sees; each ift cell compiles the spectral and gradient-bound machinery once (~6 s cold, nearly all XLA compile)
tests/core/test_coupling_diagnostics_leave_the_state_alone.py::test_diagnostics_leave_the_returned_state_bit_identical[jacobi-mixed-ift] # kept: diagnostics=True must leave the returned state bit-identical, a claim a compiler rewrite can break and only a bit-for-bit comparison on every jaxlib sees; each ift cell compiles the spectral and gradient-bound machinery once (~6 s cold, nearly all XLA compile)
tests/core/test_coupling_fixture_invariants.py::test_every_l2_configuration_reaches_the_same_fixed_point[heterogeneous-2000] # kept: the per-push witness that every accelerator lands on the same fixed point on a group with four decades of scale (the shape that hid a 20.6% error), now that the generated-graph property is slow-marked; six configurations, each a compile of its own (12-17 s cold)
tests/core/test_coupling_solver_equivalence.py::test_a_converged_multirate_group_returns_one_state_from_either_solver # kept: the only per-push check that ift and fori return one state for a subcycled, waveform-relaxed group at the norm's float32 resolution; it pays for the multirate pair of graphs (~7 s cold, mostly tracing) that the residual-resolution test and two class-sweep cells then reuse
tests/nodes/adaptive/test_wavelet_node.py::test_eager_jit_and_graph_select_the_same_set_where_a_mirror_symmetry_ties_residuals[periodic-2d] # kept: the first test in its file to build a 2-D four-level wavelet node; the ~5 s of eager compiles it pays are reused by the theta sweep after it, so marking it slow only moves them (measured locally: the sweep goes from 5.4 s to 10.9 s)
tests/property/test_coupling_error_bound.py::test_converged_implies_the_state_is_within_tolerance_of_the_fixed_point # kept: the invariant the 0.4.0 convergence criterion exists for (converged=True puts the state within tolerance of an independently re-solved fixed point), stated over generated graphs; the graph is the draw, so every example compiles its own and nothing can be shared (80-100 s cold); the example in tests/core/test_coupling_error_bound.py cannot rule out a configuration nobody built
tests/compliance/test_gate_scripts.py::TestTransformGate::test_the_repository_transform_references_all_resolve # kept: pays for the one transforms-gate run on the repository (a module fixture, ~5 s of imports) that three TestTransformLiveRegistration tests also read; together they are the only check that the gate verified references rather than degrading on an importable module
tests/core/test_compile_counts.py::test_the_committed_baseline_matches_what_the_code_compiles_to # kept: the compile-count gate itself (one scripts/compile_counts.py --check); no CI job runs it otherwise, and an extra retrace per step is silent everywhere else
